type: object
required:
  - code
  - message
properties:
  code:
    type: string
    enum:
      - USER_NOT_FOUND
      - TASK_NOT_FOUND
      - INVALID_INPUT
      - INTERNAL_ERROR
    example: USER_NOT_FOUND
  message:
    type: string
    example: User not found
